schema_version = 1

[build_system]
requirements = [
  "setuptools==84.0.0",
  "wheel==0.45.1",
]

[[approved_sdists]]
name = "distance"
version = "0.1.3"
owner = "ai-auto-lrc maintainers"
reason = "g2p-en requires this pure-Python release and upstream publishes no wheel"
expires = "2027-03-04"
rebuild_command = "python -m pip wheel --no-index --find-links build-system --no-deps distance-0.1.3.tar.gz"

[[approved_sdists]]
name = "julius"
version = "0.2.7"
owner = "ai-auto-lrc maintainers"
reason = "core resampling uses this pure-Python release and upstream publishes no wheel"
expires = "2027-03-04"
rebuild_command = "python -m pip wheel --no-index --find-links build-system --no-deps julius-0.2.7.tar.gz"

[[approved_sdists]]
name = "kroman"
version = "1.1"
owner = "ai-auto-lrc maintainers"
reason = "Korean romanization uses this pure-Python release and upstream publishes no wheel"
expires = "2027-03-04"
rebuild_command = "python -m pip wheel --no-index --find-links build-system --no-deps kroman-1.1.tar.gz"
